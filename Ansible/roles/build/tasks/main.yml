---
# tasks file for build
                          tasks:
                          - name: build app
                            shell: ng build --production
                            args: 
                              chdir="~/project/dir/"
